{
    "build": {
    "builder": "node16",
    "buildCommand": "npm install && npm run build",
    "startCommand": "npx serve -s build -l 3000"
    },
    "deployments": {
    "rootDirectory": "."
    }
}
